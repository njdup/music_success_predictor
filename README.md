Music Commercial Success Predictor
---------------------------------

Data Collection
===============

To run, first install requirements with `pip install -r requirements.txt`
Then, run the data collection script by saying `python data-collector.py` within the data/ directory
